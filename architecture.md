# Architecture: Quarkus + GraalVM + hono/html HDA demo

## Overview

A **Hypermedia-Driven Application (HDA)**: HTML is rendered server-side and the browser swaps in
fragments with htmx — no SPA, no client-side rendering.

- **Quarkus 3.32.4** (Java 21) — JAX-RS resources (`quarkus-rest`), CDI (`quarkus-arc`), JDBC
  persistence
- **hono/html** — TypeScript components written with hono's `html` tagged-template function,
  bundled by esbuild into a single `ssr.js`
- **GraalVM Polyglot 25.0.2** — runs `ssr.js` inside the JVM at request time. The `polyglot` / `js`
  artifacts are plain libraries (work on a stock JDK); a GraalVM JDK is used at runtime so GraalJS
  JIT-compiles hot JS instead of interpreting it. No native image.
- **htmx 4 + hyperscript** — browser-side partial updates

The templates are plain functions — `(vm: SomeModel): HtmlResult => html`…`` — not JSX. `String(...)`
on the result yields the HTML. There is no `hono/jsx`, no `renderToString`, no virtual DOM anywhere.

---

## Project structure

```
project-root/
├── pom.xml                    # Quarkus 3.32.4, GraalVM polyglot/js 25.0.2, typescript-generator, gmavenplus
├── package.json               # esbuild + hono (only)
├── tsconfig.json              # { "strict": true }  — no jsx settings
├── watch.ts                   # Bun watcher: re-bundles ssr.js on any .ts change under src/main/java
├── src/main/java/dev/svenehrke/demo/
│   ├── core/
│   │   ├── config/            # AppConfig, DevConfig (@ConfigMapping), QuarkusProfile
│   │   ├── PeopleService.java  PeopleRepository.java
│   ├── inbound/web/
│   │   ├── *.ts               # hono/html components + routing + shared types (see below)
│   │   ├── generated/types/   # vm-types.d.ts, web-api-consts.ts   (git-ignored, regenerated by mvn)
│   │   ├── infra/js/          # GraalVM integration layer
│   │   ├── form/PersonEditForm.java     # @BeanParam target for the PUT
│   │   ├── *Resource.java     # RootResource, PersonUIResource, PersonActionResource
│   │   ├── *Model.java        # PersonPageModel, PersonTableModel, PersonDetailModel, PersonEditModel, PersonTableRowModel
│   │   ├── JTSPersonRouteName.java   JTSPersonEventName.java   HonoWebApiSharedConsts.java   HTMXConsts.java
│   └── outbound/db/           # H2PeopleRepository, DBInitializer (Datafaker seed 0)
├── src/main/resources/
│   ├── application.properties       # app.ssr.resource=graaljs/ssr.js
│   ├── application-dev.properties
│   ├── db/migration/V1__create_person_table.sql   (Flyway)
│   └── META-INF/resources/          # bulma.min.css, htmx 4, hyperscript 0.9.93, alpine 3.16.3, main.js, dev.js
├── src/main/docker/                 # Quarkus-generated Dockerfile.{jvm,native,native-micro,legacy-jar}
└── target/classes/graaljs/ssr.js   # esbuild output — a plain classpath resource, not under a served path
```

`.ts` files live next to the Java web layer on purpose: the templates are part of the web tier, not
a separate frontend project. (`src/main/resources/templates/home.html` is a leftover from the
scaffold and unused.)

---

## Request pipeline

```
Browser  GET /
  → RootResource.redirect()                       → 303 See Other → /uiroute/Page

Browser  GET /uiroute/Page
  → PersonUIResource.uiroute("Page")
      → JTSPersonRouteName.valueOf("Page")         [unknown name → 404 NotFoundException]
      → PeopleService.personTableModel()           [SQL → Java record]
      → new PersonPageModel(tableModel)            [assemble view model]
      → JsxRenderer.render(JTSPersonRouteName.Page, vm)
          → JsonUtil.toJson(vm)                    [VM → JSON string, via JSON-B]
          → pool.borrow()                          [acquire a GraalVM Context]
          → ctx.getEntryFunction("render")
               .execute("Page", vmJson)            [cross-language call]
              → render.ts: personRoutes["Page"].render(JSON.parse(vmJson))
              → String(Layout(html`… Page(vm) …`))   [hono/html → HTML string]
          → result.asString()
          → pool.release(ctx)
      → return String                             [JAX-RS writes the HTTP response body]
```

**JSON is the only Java↔JS contract.** View models are serialized to a JSON string, passed across
the GraalVM boundary as a plain string, and `JSON.parse`'d in TypeScript. No GraalVM `Value`
proxying, no Java↔JS object bridging.

`PersonTable` is the one route that needs a `search` param instead of `id`, so it has its own
`@GET @Path("/PersonTable")` method rather than growing `uiroute()`'s signature; JAX-RS matches the
literal path before the `/{name}` template. The `uiroute()` switch's `default` branch throws
`IllegalStateException` for any route that has such a dedicated endpoint.

---

## GraalVM integration layer — `inbound/web/infra/js/`

| Class | Role |
|---|---|
| `JsHolder` | `@ApplicationScoped @Startup`. Owns one shared `Engine` (created at startup — lets GraalVM JIT hot JS across contexts). `buildSource()` reads `ssr.js` from `app.ssr.resource` via `getClassLoader().getResourceAsStream(...)`; `buildPool()` creates the `SimplePool`. `initPool()` discards and rebuilds the pool on hot-reload. |
| `JsConnection` | One pool slot = one GraalVM `Context`. On construction: inject a `TextEncoder` polyfill (hono needs it, GraalVM doesn't provide it), inject `var module = {exports:{}}; var exports = module.exports;` for CommonJS, `eval` the whole `ssr.js`, then scan `module.exports` for callables into a `Map<String, Value>`. |
| `SimplePool<T>` | `ArrayBlockingQueue` sized to `availableProcessors()`. `borrow()` blocks until a slot frees — GraalVM `Context`s are **not** thread-safe. |
| `JsxRenderer` | The CDI bean. `render(JTSPersonRouteName route, Object vm)`: serialize `vm` to JSON (`JsonUtil`, JSON-B), borrow a slot, `getEntryFunction("render").execute(route.name(), vmJson)`, `asString()`, release. |

---

## TypeScript layer — `inbound/web/*.ts`

esbuild bundles everything reachable from `render.ts` into one `target/classes/graaljs/ssr.js`
(`--bundle --platform=neutral --format=cjs`). `platform=neutral` keeps Node/browser globals out;
`format=cjs` matches the `module.exports` shim.

### Entry point — `render.ts`

```ts
export function render(route: string, vmJson: string): string {
  const routeDefinition = personRoutes[route];
  if (routeDefinition) return String(routeDefinition.render(JSON.parse(vmJson)));
  return String(html`<div>ROUTE '${route}' NOT FOUND</div>`);
}
```

It does **not** dispatch itself — it looks `route` up in `routes.ts`'s `personRoutes` map. `String(...)`
at this boundary is required: `html`…`` returns a boxed `HtmlEscapedString` (possibly a `Promise`),
and `JsxRenderer` calls `Value.asString()`, which needs a primitive string. `HtmlResult` everywhere
inside the components; stringify exactly once here (see `render.ts`'s header comment).

### Files

| File | Role |
|---|---|
| `layout.ts` | Root HTML shell — `<html><head>` with bulma, htmx, hyperscript, alpine, `main.js`, `dev.js` |
| `personpage.ts` | Full page: search `<input>` + `PersonTable` |
| `persontable.ts` | `<div id="result-table">` — table + bulk-delete form |
| `personrow.ts` | One `<tr>`, htmx expand-on-click |
| `persondetails.ts` | Composite: `PersonDetailsRow` + `PersonDetailsCard` |
| `persondetailrow.ts` | Expanded header row, hyperscript collapse |
| `persondetailscard.ts` | Details card; click → edit form |
| `personedit.ts` | Edit-form row: htmx `PUT` + hyperscript "back" |
| `render.ts` | GraalVM entry point (above) |
| `routes.ts` | `personRoutes` — the single map of `{ url, render }` per route, `satisfies Record<JTSPersonRouteName, RouteDefinition>`; also `personActionUrls` for the mutation endpoints |
| `route-types.ts` | `HtmlResult = ReturnType<typeof html>`, `RouteDefinition`, `ActionUrlDefinition` |
| `jtsperson.ts` | `eventName(name: JTSPersonEventName)` identity guard — every event-name string goes through it |
| `generated/types/vm-types.d.ts` | generated — VM interfaces + `JTSPersonRouteName` / `JTSPersonEventName` string-union types |
| `generated/types/web-api-consts.ts` | generated — `export const HonoWebApiConsts = { PERSON, DELETE } as const` |

---

## Java → TS generation (Java is the source of truth)

Two `process-classes`-phase Maven plugins keep the TS side in sync; both outputs are git-ignored and
rebuilt on every `mvn package` (`mvn compile` stops one phase too early).

1. **`cz.habarta.typescript-generator`** — scans `*Model` / `*VM` classes and the enums
   `JTSPersonRouteName`, `JTSPersonEventName`, and writes `generated/types/vm-types.d.ts`
   (`mapEnum: asUnion`, `outputFileType: declarationFile`). Types only, no runtime values.
2. **`org.codehaus.gmavenplus` inline Groovy script** (in `pom.xml`) — reflects over
   `HonoWebApiSharedConsts.HonoWebApiConsts` and writes `generated/types/web-api-consts.ts` with the
   two mutation path templates as **runtime** `const` strings (needed because `routes.ts` does
   `HonoWebApiConsts.PERSON.replace('{id}', id)`). Needs `gmavenplus-plugin` 5.1.0 +
   `org.apache.groovy:groovy` 5.1.1 on JDK 25.

So: add a route → add the `JTSPersonRouteName` enum value, add its `case` in `PersonUIResource`, add
the `personRoutes` entry in `routes.ts` (the `satisfies` makes a missing entry a TS error).

---

## htmx / hyperscript choreography

- `hx-get` / `hx-put` / `hx-delete` on elements → partial swaps.
- Hyperscript `_="…"` attributes → client-side DOM events, no JS files.
- `<template hx-trigger="…">` — invisible htmx triggers embedded in rows, listening for custom events.

Event names come from `JTSPersonEventName` (`PERSON_UPDATED`, `PersonDetailsRow_CloseCmd`,
`PersonEditor_CloseRequested` — the last two `// TS-only`). `.ts` code never writes an event name as
a bare string; it calls `eventName('…')`, which typechecks against the generated union. Names use
`UPPER_SNAKE` / `Word_Word` — both safe to `send` unquoted in hyperscript (a hyphenated name is not).

**Saving an edit:**
1. Save button fires `PUT /person/{id}` with `hx-swap="none"`.
2. `PersonActionResource` sets `HX-Trigger: {"PERSON_UPDATED": {"id": 5}}`.
3. htmx dispatches a `PERSON_UPDATED` DOM event `{id: 5}`.
4. A `<template hx-trigger="PERSON_UPDATED[detail.id === 5] …">` inside the row re-fetches the row.

`HTMXConsts.java` holds the response-header name constants.

---

## Dev hot-reload (`quarkus.profile=dev`)

1. **`watch.ts`** (Bun) watches `src/main/java/**/*.ts` → runs esbuild → rewrites
   `target/classes/graaljs/ssr.js`.
2. **`JsBundleWatcher`** (`@IfBuildProfile("dev")`, `@Scheduled(every = "1s")`) polls
   `dev.ssr.filename`'s `lastModified`; on change → `JsBundleChangeHandler.run()` →
   `jsHolder.initPool()` (rebuild the Context pool) + `DevReloadSSE.broadcastReload()`.
3. **`DevReloadSSE`** (`@IfBuildProfile("dev")`, Vert.x `@Route("/dev-reload")`) pushes a `reload`
   event; `META-INF/resources/js/dev.js` listens with `new EventSource("/dev-reload")` and calls
   `location.reload()`.

Only the JS `Context` pool is rebuilt — the JVM keeps running.

---

## Persistence

- **H2** in-memory (`jdbc:h2:mem:demo`), **Flyway** migration `V1__create_person_table.sql`
  (`quarkus.flyway.migrate-at-start=true`, `quarkus.flyway.locations=db/migration`).
- **`H2PeopleRepository`** returns typed Java records.
- **`DBInitializer`** (`@Startup @ApplicationScoped`, `@PostConstruct`) seeds 150 people via raw JDBC
  with `net.datafaker.Faker` on `new Random(0)` — the Playwright suite relies on the deterministic
  first person ("Jackie Rau").

---

## Key design decisions

| Decision | Rationale |
|---|---|
| hono `html` tagged templates (not JSX) for SSR | Plain functions, no JSX toolchain/`jsxImportSource`, `String()` = HTML |
| JSON string as the only Java↔JS contract | Simple, debuggable, no GraalVM `Value` proxying |
| Blocking pool of GraalVM `Context`s | `Context` is not thread-safe |
| Java as the source of truth, TS generated | Route names, event names and VM shapes can't drift; a rename on the Java side is a TS compile error |
| `.ts` co-located with the Java web layer | Templates are the web tier, not a separate project |
| GraalVM as a library (GraalVM JDK only for JS JIT), no native image | Standard Maven build; the polyglot API works on any JVM |
